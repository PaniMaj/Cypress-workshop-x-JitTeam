{
  "name": "jit-cypress",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "testOnChromeOpen": "npx cypress run --headed --browser chrome",
    "testOnChromeHeadless": "npx cypress run --browser chrome",
    "testOnEdge": "npx cypress run --browser edge",
    "edgeDevEnv": "npx cypress run --headed --browser edge --config baseUrl=http://docs.cypress.io"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.6.0",
    "cypress-mochawesome-reporter": "^3.7.0",
    "dotenv": "^16.3.1",
    "mochawesome": "^7.1.3"
  }
}
